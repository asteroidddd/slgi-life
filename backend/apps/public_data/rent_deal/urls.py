"""rent_deal public-data URL module.

The current UI does not expose raw transaction pins on the map, so this module
keeps the include target importable without publishing transaction endpoints.
"""

app_name = "rent_deal"

urlpatterns = []
